{
  "name": "brighterscript-template",
  "version": "1.0.0",
  "description": "Roku's best roguelike (excluding Nethack)",
  "author": "Morgan LaVigne",
  "scripts": {
    "build": "bsc --createPackage=false",
    "watch": "npm run build -- --watch",
    "package": "bsc --create-package"
  },
  "devDependencies": {
    "brighterscript": "^0.65.2"
  }
}
